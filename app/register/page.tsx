import { RegisterPortal } from '@/components/auth/RegisterPortal';

export const metadata = { title: 'Create Account | ToyVerse' };

export default function RegisterPage() {
  return <RegisterPortal />;
}
